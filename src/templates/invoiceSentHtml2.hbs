<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice - Bold Header</title>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap"
        rel="stylesheet">
    <style>
        :root {
            --primary: #0f172a;
            /* Dark Navy */
            --accent: #d97706;
            /* Orange/Gold */
            --text: #334155;
            --light: #f8fafc;
            --border: #e2e8f0;
        }

        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            background: #e2e8f0;
            padding: 40px;
            color: var(--text);
            -webkit-print-color-adjust: exact;
            margin: 0;
        }

        .invoice-box {
            max-width: 800px;
            margin: auto;
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }

        /* HEADER */
        .header {
            background: var(--primary);
            color: white;
            padding: 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo-section img {
            height: 50px;
            background: white;
            padding: 5px;
            border-radius: 4px;
        }

        .invoice-title {
            font-size: 30px;
            font-weight: 800;
            text-transform: uppercase;
            text-align: right;
        }

        .invoice-details {
            text-align: right;
            font-size: 12px;
            opacity: 0.8;
            margin-top: 5px;
        }

        /* BODY */
        .body-section {
            padding: 40px;
        }

        .grid-info {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
            margin-bottom: 40px;
        }

        .info-title {
            font-size: 11px;
            text-transform: uppercase;
            font-weight: 700;
            color: #94a3b8;
            margin-bottom: 8px;
        }

        .info-content {
            font-size: 14px;
            font-weight: 600;
            color: var(--primary);
        }

        .info-sub {
            font-size: 13px;
            font-weight: 400;
            color: var(--text);
            line-height: 1.5;
            margin-top: 4px;
        }

        /* TABLE */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
        }

        th {
            text-align: left;
            padding: 15px 10px;
            background: var(--light);
            color: var(--primary);
            font-size: 11px;
            text-transform: uppercase;
            border-bottom: 2px solid var(--border);
        }

        td {
            padding: 15px 10px;
            border-bottom: 1px solid var(--border);
            font-size: 13px;
        }

        th:last-child,
        td:last-child {
            text-align: right;
        }

        /* TOTAL */
        .total-section {
            display: flex;
            justify-content: flex-end;
        }

        .total-box {
            width: 40%;
        }

        .row {
            display: flex;
            justify-content: space-between;
            padding: 5px 0;
            font-size: 13px;
        }

        .big-total {
            border-top: 2px solid var(--primary);
            margin-top: 10px;
            padding-top: 10px;
            font-weight: 800;
            color: var(--primary);
            font-size: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        @media print {
            body {
                padding: 0;
            }

            .invoice-box {
                box-shadow: none;
                border-radius: 0;
            }
        }
    </style>
</head>

<body>
    <div class="invoice-box">
        <div class="header">
            <div class="logo-section">
                <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTLqNNWjHVl3zISVsJ_sWApwO9GtsR8MYPtJg&s">
                <div style="margin-top:10px; font-weight:700;">The House of Vastra</div>
            </div>
            <div>
                <div class="invoice-title">Tax Invoice</div>
                <div class="invoice-details">#55 • 20 Mar 2024</div>
            </div>
        </div>

        <div class="body-section">
            <div class="grid-info">
                <div>
                    <div class="info-title">Billed To</div>
                    <div class="info-content">Ronak Panchal</div>
                    <div class="info-sub">Gujarat, India<br>POS: Gujarat (24)</div>
                </div>
                <div>
                    <div class="info-title">Company Details</div>
                    <div class="info-content">GSTIN: 26CORPP3939N1ZA</div>
                    <div class="info-sub">Mumbai, Maharashtra - 400001</div>
                </div>
            </div>

            <table>
                <thead>
                    <tr>
                        <th>Item</th>
                        <th>Qty</th>
                        <th>Rate</th>
                        <th>Tax</th>
                        <th>Amount</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>Basic Knit Top SS</td>
                        <td>1</td>
                        <td>189.52</td>
                        <td>9.48</td>
                        <td>199.00</td>
                    </tr>
                    <tr>
                        <td>Premium Cotton Tee</td>
                        <td>1</td>
                        <td>189.52</td>
                        <td>9.48</td>
                        <td>199.00</td>
                    </tr>
                </tbody>
            </table>

            <div class="total-section">
                <div class="total-box">
                    <div class="row"><span>Taxable</span><span>379.04</span></div>
                    <div class="row"><span>IGST</span><span>18.96</span></div>
                    <div class="big-total">
                        <span>Total Due</span>
                        <span style="color: var(--accent);">₹398.00</span>
                    </div>
                </div>
            </div>

            <div
                style="margin-top: 40px; padding-top: 20px; border-top: 1px solid var(--border); font-size: 11px; display: flex; justify-content: space-between;">
                <span>Powered by Rentify</span>
                <span>Authorized Signatory</span>
            </div>
        </div>
    </div>
</body>

</html>