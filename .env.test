# Test Environment Configuration
NODE_ENV=test
LOG_LEVEL=error

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_test_password
DB_NAME=rentify_test
DB_QUEUE_LIMIT=0

# JWT Configuration
JWT_SECRET=test-jwt-secret-key-minimum-32-characters-long
JWT_EXPIRES_IN=7d

# Token Configuration
TOKEN_SIGNING_KEY=test-token-signing-key-min-32-chars
SESSION_ENCRYPTION_KEY=test-session-encryption-key-32-characters
ACCESS_TOKEN_EXPIRES_MIN=15

# Email Configuration (for testing)
EMAIL_HOST=smtp.mailtrap.io
EMAIL_PORT=2525
EMAIL_USER=test_user
EMAIL_PASSWORD=test_password
EMAIL_PROVIDER=smtp

# Cookie Configuration
COOKIE_HTTP_ONLY=false
COOKIE_SECURE=false
COOKIE_SAMESITE=Lax
COOKIE_DOMAIN=localhost

# Test Configuration
SILENT_TESTS=false